import * as React from 'react';

export const route = '*';

const Page: React.FC = () => (
  <>
    <h1>Not Found Page</h1>
    <p>The requested page was not found.</p>
  </>
);

export default Page;
